<%= provide(:title, 'Lunch') %>
<div class="row">
	<div class="span8">
      	<h3>Restaurants (<%= @lunch.restaurants.count %>)</h3>
      	<%= render 'shared/feed' %>
		<%= render 'restaurant_form' %>
    </div>
</div>